// 0 - 100
export enum DefaultAudioSetting {
  MUSIC_VOLUME = 80,
  SOUND_VOLUME = 80,
}

export enum DefaultKeyboardKeysSettings {
  UP = "w",
  LEFT = "a",
  RIGHT = "d",
  DOWN = "s",
  JUMP = " ",
}
